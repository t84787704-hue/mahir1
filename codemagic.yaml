workflows:
  android-workflow:
    name: Android Build

    environment:
      flutter: stable

    working_directory: app

    scripts:
      - flutter pub get
      - flutter build apk --release

    artifacts:
      - build/app/outputs/flutter-apk*.apk